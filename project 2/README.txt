> First open the project in VSCode 
> In terminal run this command to create virtual enviorment
	python -m venv venv

>Then close the teminal and open the terminal again 
	shortcut key: CTRL + Shift+ { key above the tab button } 

> Then install the libraries required for this specific project in the virtual enviorment for this run this command
	pip install Flask  pywin32 SpeechRecognition gtts pyaudio opencv-python pygame pyjokes pypiwin32

>To run the project run this command
	python app.py

> It will give you a URL Click on that or in the browser write 
	http://127.0.0.1:5000
